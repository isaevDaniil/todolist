//const ADD_TASK = "ADD_TASK";

let initialState = {
    
}

const headerReducer = (state = initialState, action) => {
    switch (action.type) {
        case "asd":
            
            break;   
        default:
            return state;
    }
}

export default headerReducer;